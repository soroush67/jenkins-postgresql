# Rendered by roles/postgres - generated by Ansible, do not edit by hand.
# Nothing here needs per-deployment substitution (auth is carried via
# DATA_SOURCE_NAME, not this file) - templated anyway for consistency
# with everything else this role renders, and so a future real option
# here doesn't need a second mechanism.
auth_modules: {}
