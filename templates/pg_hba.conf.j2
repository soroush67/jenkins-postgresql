# Rendered by roles/postgres - generated by Ansible, do not edit by hand.
#
# The original file had `local all all trust` - unauthenticated,
# passwordless, superuser-equivalent access to anyone who can get a
# shell in the container (e.g. `docker exec`). Hardened to
# scram-sha-256, matching the other two rules that were already
# password-protected.
local   all             all                                     scram-sha-256
host    all             all             127.0.0.1/32            scram-sha-256
host    all             all             {{ pg_hba_allowed_cidr }}            scram-sha-256
